stages:
  - build
  - analyse
  - test
  
build-reqs:
  stage: build
  script:
    - python -m pip install --upgrade pip
    - pip install pylint
    - pip install -r requirements.txt

analyse-1:
  stage: analyse
  script: 
    - pylint transform.py
    - pylint main.py
    
test-1:
  stage: test
  script:
    - echo "Compiling and running tests:"
    - python test.py
